<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mounae">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".connect_bank_onboarding"></activity>
        <activity android:name=".devise_code_1" />
        <activity android:name=".first_name" />
        <activity android:name=".otp" />
        <activity android:name=".inputphone_signin" />
        <activity android:name=".inputphone_signup" />
        <activity android:name=".onboardingthree" />
        <activity android:name=".onboardingtwo" />
        <activity android:name=".onboardingone">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>